name: Post-Merge Verification
on:
  workflow_run:
    workflows: ["Cypress Tests"]
    branches: [main]
    types:
      - completed

jobs:
  post-merge-verification:
    runs-on: ubuntu-latest
    steps:
      - name: Verify main branch stability
        run: echo "All tests passed in previous workflow - Production ready!"